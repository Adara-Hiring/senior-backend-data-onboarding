def get_by_dpid(dpid):
    """
    This function responds to a request for /api/v1.0/intel/{dpid}
    with a list of cookies and scores conntected to this data provider

    :param dpid:    the dpid to filter the scores.
    :return:        list of cookies and scores for a dpid
    """

    return
